# TrainWatcher 追跡パネル ルート2追加 引き継ぎ

作成: 2026-08-03
状態: **実装・取得・埋め込み・全量検証すべて完了 / 残りはブラウザ目視確認とコミットのみ**

新セッションはこのファイルと `HANDOFF_tracker.md`（ルート1の経緯）を読めば追いつける。

## スコープ

追跡パネルに「ルート2」を追加する。既存のルート1（秦野〜相模大野〜中央林間〜南町田）は
一切変更しない。

- ルート2: 秦野 〉 海老名 〉 横浜 〉 馬車道 と、その帰り
- 追跡ボタンを **1ボタン3状態循環**（`追跡 OFF` → `追跡 ON` → `追跡 ON (r2)` → OFF）
- みなとみらい線は**観測だけ**行い、カード・ランクサマリーには出さない（追跡パネルの「!」のみ）

## 確定事項

### ユーザー決定（2026-08-03）

| 論点 | 決定 |
|---|---|
| 追跡ボタン | 1ボタンで3状態を循環 |
| ルート2のボード数 | 往復3駅ずつの計6ボード |
| みなとみらい線の遅延 | バックグラウンド取得と「!」だけ。ランクサマリー・カードの表示は変えない |
| 目的駅に行かない列車 | **現仕様のまま一覧に残す**（到着列に「不可」） |

### アプリ名とバージョン（2026-08-03 変更）

- 表示名を **TrainWatcher → TrainNow** に変更。画面左上は `TrainNow 1.00`
- **機能が増えるたびにバージョンを 0.01 上げる**（1.00 → 1.01 → 1.02 …）。
  `<span class="navbar-title">` の数字を必ず更新すること
- `<title>` は `TrainNow`（バージョンなし）
- **ファイル名 `index.html`・フォルダ名 `TrainWatch` は変更していない**（git 履歴とリンクのため）

### ルート2の配色

ルート2を選択している間だけ、追跡ボタンと選択中の駅ボタンを
**ライトグリーン `#9CCC65`** にする（ルート1はシアン `#26C6DA` のまま）。

ランクの平常色 `#34A853` とは別の、黄寄りで明るい緑を選んでいる。
同じ緑にするとボタンが「路線の状態表示」に見えてしまうため。

CSS は `#tracker-toggle.on.r2` と `.tracker-st-btn.sel.r2`。
`r2` クラスは `renderTrackerControls()` が `trackerOn === 2` の時だけ付ける。
方向ボタン（△行き/▼帰り）は指定がないので薄紫 `#CE93D8` のまま。

### 6ボード定義（実測した Yahoo の ID）

| ID | 方向 | 駅 | 路線・方面 | 駅ID/方面ID | 到着列 |
|---|---|---|---|---|---|
| R2G1 | 行き | 秦野 | 小田急小田原線 新宿・町田 | 23283/3090 | 海老名着 |
| R2G2 | 行き | 海老名 | 相鉄本線 横浜・新宿 | 23088/3440 | 横浜着 |
| R2G3 | 行き | 横浜 | みなとみらい線 元町・中華街 | 23368/6871 | 馬車道着 |
| R2K1 | 帰り | 馬車道 | みなとみらい線 渋谷・和光市 | 29481/6870 | 横浜着 |
| R2K2 | 帰り | 横浜 | 相鉄 海老名・湘南台 | 23368/3441 | 海老名着 |
| R2K3 | 帰り | 海老名 | 小田急小田原線 小田原・本厚木 | 23088/3091 | 秦野着 |

**R2G1 は既存 G1 と同一 URL**（秦野 23283/3090）。詳細ページのキャッシュをそのまま流用でき、
到着駅を海老名に変えて再パースするだけで済む（ネットワーク取得ゼロ）。

みなとみらい線の運行情報 詳細ページ ID は **401**（相鉄線は既存の 125 と同じ）。

### 所要時間の検算（ユーザー提示の乗換案内と一致）

- 横浜 → 馬車道 5分（提示: 11:30発 → 11:35着）
- 秦野 → 海老名 21分 / 海老名 → 秦野 22分
- 海老名 → 横浜 34分（各停。提示の特急は28分）

### 実測した列車本数（2026-08-03 取得・合計 2,672本）

| ボード | 平日 | 土休日 | 到着なし(平日/土休日) |
|---|---|---|---|
| R2G1 秦野→海老名 | 131 | 130 | 14 / 23 |
| R2G2 海老名→横浜 | 206 | 184 | 90 / 87 |
| R2G3 横浜→馬車道 | 298 | 273 | 31 / 58 |
| R2K1 馬車道→横浜 | 266 | 212 | 0 / 0 |
| R2K2 横浜→海老名 | 246 | 213 | 128 / 118 |
| R2K3 海老名→秦野 | 267 | 246 | 142 / 134 |

ルート1（3,185本）と合わせて `index.html` には **14ボード・5,857本**が入っている。
ネットワーク取得は 2,381回（R2G1 はキャッシュ流用で0回）。パースエラーは0件。

### 種別の略称：通勤特急を「通勤」と出さないこと

`build.py` の `TYPE_SHORT` に無い種別は `name[:2]` で切られる。
**東急東横線の「通勤特急」がそのままだと「通勤」と表示される**ため、
`build_r2.py` の `TYPE_SHORT_ADD = {'通勤特急': '通特'}` で上書きしている。

全ボードの凡例を全量確認した結果、略し漏れはこの1件だけだった（他7種別は正しい）。
`index.html` の `TRACKER_TYPE_CLASS` にも `'通特': 'tr-ty-ltd'`（特急と同じ紫）を追加済み。
TT_TYPES 8種別すべてに色クラスがあることを確認済み。

### 目的駅に行かない列車が多い理由

- R2G2（206本中90本）: 方面「横浜・新宿」に**相鉄新横浜線経由の新宿方面直通**が混ざる
- R2K2: 「海老名・湘南台」に**いずみ野線 湘南台行き**が混ざる
- R2K3: 本厚木・伊勢原行きなど**秦野の手前で終着**

いずれもパーサの不具合ではない。`ride_minutes()` が `None` を返すのが正しい。

## 変更したファイル

### `index.html`（プロジェクト本体）

| 箇所 | 変更 |
|---|---|
| `TARGET_LINES` | `'みなとみらい線'` を末尾に追加 |
| `TRACKER_ONLY_LINES`（新規） | `['みなとみらい線']`。カード・サマリーから外す対象 |
| `LINE_DETAIL_ID` | `'みなとみらい線': 401` |
| `TARGET_LINE_KEYMAP` | `'みなとみらい線': ['みなとみらい']` |
| `refreshInner()` | `shownItems` を新設し、`rankSummary` と `items` はこれを使う。`lineRankMap` は全路線のまま |
| `TRACKER_BOARDS` | `{r1:{go,back}, r2:{go,back}}` の2階層に変更 |
| `TRACKER_DEFAULT_BOARD` | 同じく2階層 |
| `trackerOn` | boolean → **0/1/2**。`trackerRoute()` で `'r1'`/`'r2'` を返す |
| `toggleTracker()` | `(trackerOn + 1) % 3` の循環。ルートが変わるので既定ボードに戻す |
| `initTracker()` | localStorage が `'1'`/`'2'` の時だけ ON。**旧 `'1'` との互換あり** |

`trackerOn` を truthy 判定している箇所（`renderTrackerControls` / `renderTrackerList` / `tick`）は
0 が falsy なのでそのまま動く。参照は grep で全件確認済み。

### scratchpad の `tw_build/`（既存ファイルは1つも変更していない）

実パス:
`C:\Users\SHOTAT~1\AppData\Local\Temp\claude\C--Users-ShotaTanaka-Documents--------\7786d944-750b-4f47-bc1a-27177caa3590\scratchpad\tw_build`

| ファイル | 役割 |
|---|---|
| `boards_r2.py` | ルート2の6ボード定義 |
| `build_r2.py` | 取得・組み立て。`--smoke` で各ボード3本だけ試せる |
| `embed_r2.py` | `index.html` への埋め込み。CRLF・BOMなしを維持 |
| `verify_r2.py` | 埋め込み後の全量検証 |
| `find_ids_r2.py` / `find_diainfo_r2.py` | 駅ID・運行情報IDの調査 |
| `build_r2.log` | 取得ログ（進捗・本数・到着なし件数） |

種別・行先のプールは `out/pools.json`（ルート1）を初期値にして拡張する。
**ルート1のインデックスを動かさないため。** `embed_r2.py` と `verify_r2.py` の両方で
先頭一致を assert している。

## 未確定・未確認

- **ブラウザでの目視確認が未実施。** この環境に node/bun/deno が無く、JS構文エラーと描画は確認できない
- 遅延マークの実表示は、実際に路線が遅延していないと確認できない
- ルート2の方向自動判定はルート1と同じ（2:00〜11:59 → 行き）。ルート別に変える要望が出たら
  `defaultTrackerDir()` をルート引数付きにする

## 実施済みの検証（2026-08-03）

`verify_r2.py` を全量で通し、**NG 0件**。サンプリングはしていない。

- BOM なし / 素の LF 0（全1,665行 CRLF）
- TT_TYPES・TT_DESTS の先頭がルート1と完全一致（既存インデックスが動いていない）
- TIMETABLE のボードが14件・経路順
- ルート2 12区分すべてが `data_r2.json` と**完全一致**
- 全5,857本について 種別index・行先indexが範囲内 / 到着≧発車 / 発時刻が昇順
- `TRACKER_BOARDS`・`TRACKER_DEFAULT_BOARD` の参照IDがすべて TIMETABLE に存在
- `git diff` はルート2データの**追加12行のみ・ルート1のデータ行の削除/変更は0**

再ビルドしても件数・到着なし件数が1件も変わらないことを確認済み
（種別修正のためキャッシュから再実行。ネットワーク0回）。

## 次の作業

1. ユーザーがブラウザで目視確認
   - 追跡ボタンを3回押して OFF → ON → ON (r2) → OFF が循環するか
   - ルート2の駅ボタンが 秦野/海老名/横浜（行き）、馬車道/横浜/海老名（帰り）になるか
   - ランクサマリーに みなとみらい線 が**増えていない**こと
   - みなとみらい線のボード（横浜/馬車道）で種別が「通特」と出ること（「通勤」ではない）
2. コミット（実行はユーザー）
