# TrainNow GPS探索 設計書

作成日: 2026-08-12
対象: `work/OLDproject/TrainWatch/index.html`（単体HTML）
関連: `SPEC.md`（既存機能）/ `SPEC_tracker.md`（追跡パネル）/ `HANDOFF_route2.md`（ルート2）
バージョン: `TrainNow 1.00` → **`1.01`**

---

## 1. 目的とスコープ

追跡パネルの方向ボタン（△行き / ▼帰り）の右に **GPS探索トグル**を追加する。
ON にすると現在地から**最寄駅のボードを自動選択**し、
**歩いて間に合う列車に印**を付ける。

### やること

- `#tracker-head` に `GPS OFF / ON` トグルを1個追加（追跡パネル ON のときだけ表示）
- 現在地から最寄りの駅ボードを自動選択（ヒステリシス付き）
- 選択中の駅までの距離・徒歩時間の表示
- 時刻表リストの2行に「乗れそうな候補」印

### やらないこと

- **行き/帰りの自動判定**（手動のまま。時刻ルール `defaultTrackerDir()` を維持）
- **ルート1/2 の自動判定**（追跡ボタンの3状態循環は手動のまま）
- `watchPosition` による常時追従・乗車中判定
- 天気の現在地連動（`LOCATIONS` は変更しない）
- 既存の表示・配色・レイアウトの変更（デグレなしを最優先）

---

## 2. 前提

Geolocation API は **セキュアコンテキスト（HTTPS）でのみ動作**する。

- 本アプリは GitHub Pages（`s-tanaka-vega/trainwatch-web`）配信のため PC・iPhone とも動作する
- `file://` で直接開いた場合は PC Chrome・iPhone Safari とも**取得できない**（localhost のみ例外）
- iOS Safari は**ユーザー操作起点**が必須。トグルのタップが起点になるため条件を満たす

---

## 3. 画面仕様

### 3.1 配置

`#tracker-head` は `display:flex` なので、`#tracker-dir` の直後にボタンを1個足す。

```
[ 追跡 ON ]   △行き  ▼帰り   [ GPS ON ]
[秦野][相模大野][中央林間][南町田]
 秦野まで 徒歩 約7分（360m）
   08:04 快急 新宿  大 08:28
 ▶ 08:13 急行 新宿  大 08:41   3:21
 ┃ 08:20 急行 町田  大 08:47  10:21
   08:28 各停 本厚  大 09:01
 ┃ 08:35 快急 新宿  大 08:58
```

- 追跡パネルが OFF のときは方向ボタンと同様に**表示しない**（`display:none`）
- 情報行 `#gps-info` は `#tracker-stations` と `#tracker-list` の間

### 3.2 ボタンの状態と表示

| `gpsState` | 文言 | クラス | 色 |
|---|---|---|---|
| `off` | `GPS OFF` | （なし） | `#666` |
| `wait` | `GPS 測位中…` | （なし） | `#666` |
| `auto` | `GPS ON` | `on` | `#FFB74D` |
| `manual` | `GPS ON (手動)` | `manual` | `#B0873B` |
| `err` | `GPS 不可` | `err` | `#8d5a5a` |

配色は既存と衝突させない。
ルート1 = シアン `#26C6DA` / ルート2 = ライトグリーン `#9CCC65` / 方向 = 薄紫 `#CE93D8` は**据え置き**。
GPS は**アンバー `#FFB74D`**。

### 3.3 トグルの遷移

```
off  --タップ--> wait --測位成功--> auto
auto --タップ--> off
auto --駅ボタンを手で押す--> manual
manual --タップ--> auto （自動選択に復帰）
任意  --権限拒否--> err --タップ--> wait（再試行）
```

---

## 4. 駅座標

`STATION_GEO` を新設。値は **Wikipedia 各駅記事の記載値**（2026-08-12 取得）。

| 駅 | 緯度 | 経度 | 備考 |
|---|---|---|---|
| 秦野 | 35.37028 | 139.22583 | |
| 相模大野 | 35.53194 | 139.43778 | |
| 中央林間 | 35.50833 | 139.44417 | 小田急側。東急側とは約25m差で無視できる |
| 南町田 | 35.51139 | 139.47075 | 南町田グランベリーパーク駅 |
| 海老名 | 35.45270 | 139.39110 | 小田急・相鉄の中間。JR は約200m北西だが同一駅として扱う |
| 横浜 | 35.46611 | 139.62250 | |
| 馬車道 | 35.45014 | 139.63617 | |

距離計算は **Haversine**。**外部API・逆ジオコーディングは使わない（通信0回）**。

既存の `LOCATIONS`（天気用の秦野・南町田の座標）は**別物として残す**。
天気は市の代表点、GPS は駅の座標であり用途が違うため統合しない。

---

## 5. 測位ロジック

### 5.1 取得オプション

```js
{ enableHighAccuracy: false, maximumAge: 90000, timeout: 10000 }
```

`enableHighAccuracy: false` により GNSS チップを起こさず WiFi / 基地局測位を使う。
電池消費に最も効くのは間隔ではなくこのフラグ。
`maximumAge: 90000` により、90秒より新しい値があれば**測位自体が起きない**。

### 5.2 適応間隔

| 条件 | 次回間隔 |
|---|---|
| 選択中の駅まで 150m 以内（もう着いている） | 300秒 |
| 次発まで10分以内 かつ 駅まで300m超 | 60秒 |
| 上記以外 | 120秒 |
| 前回から 50m 未満しか動いていない | 直前の間隔 × 1.5（上限300秒） |

駅に着いても**完全停止はしない**。停止すると駅を離れたときに復帰できないため、
間隔を伸ばすだけにする。

### 5.3 停止条件

- `document.hidden` の間は測位しない（タイマーは回すが `getCurrentPosition` を呼ばない）
- `displayMode !== 'both'`（天気モード・CDモード）の間も測位しない
- `visibilitychange` の復帰時に即1回取得する

既存の1秒タイマー `tick()` および 5分タイマー `autoFetch` には**一切触らない**。
GPS は独立した `setTimeout` チェーンで回す。

### 5.4 エラー処理

- `PERMISSION_DENIED`（`code === 1`）→ `err` にして定期取得を停止、`localStorage` を `'0'` に戻す
- `TIMEOUT` / `POSITION_UNAVAILABLE` → **状態は変えず**、前回の位置を保持したまま次の間隔で再試行
- `navigator.geolocation` が無い → `err`

---

## 6. 最寄駅の自動選択

- 対象は **現在の追跡ルート・現在の方向のボード群だけ**（`TRACKER_BOARDS[route][dir]`）
- 各駅までの距離を Haversine で求め、最小のボードを選ぶ
- **ヒステリシス 100m**：最寄りが入れ替わっても、いま選択中の駅より 100m 以上近くないと切り替えない
- 方向ボタン・追跡ボタンを押してボード群が変わったときは、その中で改めて最寄りを選び直す
- **手で駅ボタンを押したら `manual` に落ち、以後は自動選択しない**。
  GPS ボタンをもう一度押すと `auto` に戻る

---

## 7. 徒歩ETA と「乗れそうな候補」印

### 7.1 計算式

```
徒歩分       = ceil(直線距離 × 1.3 ÷ 80) + 1
出発可能時刻 = 現在時刻 + 徒歩分
```

- 徒歩速度 **分速80m**（不動産表示基準）
- 迂回係数 **1.3**（GPS が出すのは直線距離のため補正する）
- 余裕 **1分**（改札・ホームまで）
- `accuracy > 500m` のときは**徒歩分を出さず**、距離だけ表示して印も付けない

### 7.2 印を付ける行

**既存の全行はそのまま表示する。グレーアウト・色変更・並べ替えは一切しない。**
印を付けるのは次の2本だけ。

1. 発時刻 ≧ 出発可能時刻 を満たす**最初の1本**
2. 1 以降で、**目的駅に最も早く着く1本**（到着不明 `-1` の列車は除外）

- 1 と 2 が同じ行なら印は1本ぶん
- 判定範囲は**表示中の10行内**（`pickTrains()` の結果）

### 7.3 印の実装

```css
.tracker-row.tr-gps { box-shadow: inset 3px 0 0 #FFB74D; }
```

行の左端にアンバーの縦線を引く。`.tracker-row` の既存 `padding: 3px 2px` の内側に描かれるため、
**レイアウトは1pxも動かない**。カラムを増やすと iPhone 375px で横に溢れるため採らない。

### 7.5 徒歩分の換算表（式の実測）

| 直線距離 | 補正後の歩行距離 | 徒歩分 |
|---|---|---|
| 100m | 130m | 3分 |
| 200m | 260m | 5分 |
| 360m | 468m | 7分 |
| 500m | 650m | 10分 |
| 800m | 1,040m | 14分 |
| 1,000m | 1,300m | 18分 |

やや保守的（早めに家を出る側）に出る。
間に合わない列車を「間に合う」と誤って勧めないための安全側の設定。

### 7.4 既存 `tr-soonest` との併存

既存機能として、**次発以降で目的地に早く着く2本にシアンの細い下線**を引く
`.tracker-row.tr-soonest` がある（`index.html` の CSS）。

新しい印とは基準が違う。

- 既存 `tr-soonest`：徒歩時間を考慮しない。次発以降で早着の上位2本
- 新規 `tr-gps`：**歩いて間に合う列車の中で**の最速着1本

したがって**別の行を指すことがあり、両方の印が同時に出る**。
向き（左の縦線 / 下の横線）と色（アンバー / シアン）で区別できる。
既存の下線を GPS 側に合わせて変えるのは**既存挙動の変更＝デグレ**になるため触らない。

---

## 8. 情報行

`#gps-info` に1行出す。

| 条件 | 表示 |
|---|---|
| 徒歩分あり | `秦野まで 徒歩 約7分（360m）` |
| 精度不足（`accuracy > 500m`） | `秦野まで 560m（位置精度が低いため徒歩時間は非表示）` |
| GPS OFF / 位置未取得 | 空・`display:none` |

距離の書式は 1000m 未満なら10m単位、以上なら `1.2km`。

---

## 9. 実装方針（デグレ対策）

- 既存関数の**書き換えはしない**。次の3箇所に**追記のみ**行う
  - `renderTrackerControls()` … `renderGpsControls()` の呼び出し **1行**
  - `setTrackerDir()` / `setTrackerBoard()` / `toggleTracker()` … GPS 状態更新の **1〜2行**
  - `renderTrackerList()` … 印の index 計算 **1行** と行クラスへの連結 **1式**
- 新規 DOM・関数・定数はすべて `gps` 接頭辞で追加する
- **GPS OFF のとき、生成される HTML が改修前と完全一致する**ことを検証条件に入れる
- 文字コード UTF-8(BOMなし)・改行 CRLF を維持し、編集後にバイト単位で確認する
- `<span class="navbar-title">` を `TrainNow 1.01` に更新する

---

## 10. 受け入れ条件

- [ ] 追跡 OFF・天気モード・CDモードで GPS ボタンが一切出ない
- [ ] GPS OFF 時の時刻表 HTML が改修前と完全一致する
- [ ] ON で最寄駅ボードが自動選択され、`秦野まで 徒歩 約7分（360m）` が出る
- [ ] 手で別の駅を押すと `GPS ON (手動)` になり、次の測位で元の駅に戻らない
- [ ] `GPS ON (手動)` でもう一度 GPS ボタンを押すと `GPS ON` に戻り自動選択が再開する
- [ ] 歩いて間に合う最初の1本と、間に合う中で最速着の1本にアンバーの左縦線が出る
- [ ] 権限を拒否すると `GPS 不可` になり、リストは通常表示に戻る
- [ ] 画面を裏に回している間は測位が走らない
- [ ] 幅375px（iPhone）で `#tracker-head` が折り返しても崩れない
- [ ] `index.html` が UTF-8(BOMなし)・CRLF のまま
- [ ] 画面左上が `TrainNow 1.01` になっている

---

## 11. 未確認事項

- **ブラウザでの目視確認は未実施。** この環境に node / bun / deno が無く、JS構文エラーと描画は確認できない
- **実機での電池消費は未実測。** 低精度測位＋適応間隔ならオーダー的に画面点灯の消費に埋もれるという判断であり、実測値ではない
- iPhone Safari の実挙動（権限ダイアログ・精度）は実機確認が必要

---

## 12. 実施した検証（2026-08-12）

この環境に node / bun / deno が無いためブラウザ実行は不可。
代わりに次を実施した。

### 12.1 構文・構造

- `<script>` 全体（452〜1978行）を字句走査し、括弧・引用符・テンプレート
  リテラル・正規表現リテラルの対応を確認。**balanced**
- 同じチェッカを改修前の `index.html` に通して **OK**、
  意図的に `{` を1個増やした版に通して **NG** を返すことを確認（チェッカ自体の妥当性）
- GPS 関連の識別子 43 個について**未定義参照 0 件**。
  `toggleGps` は JS 内から呼ばれないが `onclick="toggleGps()"` から参照されることを確認

### 12.2 データ整合

- `TRACKER_BOARDS` の全14ボードの駅名と `STATION_GEO` を突き合わせ。
  **座標が無い駅 0 / 未使用の座標 0**
- 埋め込み座標から駅間の直線距離を算出し、営業キロと桁が合うことを確認

| 区間 | 直線距離 | 参考（営業キロ） |
|---|---|---|
| 秦野〜相模大野 | 26.30km | 約28.6km |
| 相模大野〜中央林間 | 2.69km | 約3.0km |
| 中央林間〜南町田 | 2.43km | 約2.7km |
| 秦野〜海老名 | 17.56km | 約19.3km |
| 海老名〜横浜 | 21.01km | 約24.6km |
| 横浜〜馬車道 | 2.16km | 約2.8km |

### 12.3 印ロジックの全量シミュレーション

`pickTrains()` と `gpsMarkIndexes()` を Python に移植し、
埋め込み済みの実データ（14ボード・5,857本）に対して
**全ボード × 全ダイヤ区分 × 全時刻(120〜1559分) × 徒歩1〜30分 = 1,209,600 ケース**を通した。
サンプリングはしていない。

検証した不変条件（**NG 0 件**）。

- 印の index が表示中10行の範囲内かつ次発以降であること
- 1本目の印が「発時刻 ≧ 現在時刻＋徒歩分」を満たす**最初の**列車であること
  （その手前に条件を満たす列車が無いこと）
- 2本目の印が、1本目以降で到着時刻が最小の列車であること（`-1` 除外）
- 印が3個以上付かないこと
- 到着時刻が全て `-1` のときに印が1個で収まること

### 12.4 デグレ

- `git diff` は **303行追加・2行削除**。削除は次の2行のみ
  - `TrainNow 1.00` → `1.01`（バージョン更新）
  - 行クラス連結の末尾 `;` → 継続（`tr-gps` の連結を足すため）
- 新規 ID `gps-toggle` / `gps-info`、新規クラス `tr-gps`、
  新規キー `trainwatch_gps_on` はいずれも既存と**衝突なし**（grep 全件確認）
- GPS OFF 時は `gpsMarkIndexes()` が空集合を返すため、行クラス文字列は
  改修前と**完全に同一**になる
- GPS ブロック（1651〜1916行）は起動処理（1917行〜）より前にあり、
  `initTracker()` / `applyDisplayMode()` 実行時点で `gpsState` 等は初期化済み（TDZ なし）
- `index.html` は UTF-8(BOMなし)・全行 CRLF を維持

### 12.5 未確認

- **ブラウザでの目視確認・実機動作は未実施**（実行環境が無い）
- iPhone Safari の権限ダイアログ・測位精度は実機確認が必要
- 電池消費は未実測
