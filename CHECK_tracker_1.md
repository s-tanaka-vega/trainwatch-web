# 追跡パネル 目視確認 1回目（UI挙動）

日付: 2026-07-31
対象: `index.html`（この時点ではサンプルデータ入り。実データは2回目で入る）
関連: `SPEC_tracker.md` / `PLAN_tracker.md` / `_selftest.html`

この時点で入っているのは Task 6〜9（CSS・DOM・状態管理・列車選択・描画・遅延マーク）。
時刻表は動作確認用のサンプル（G1に12本、他ボードは1本ずつ）。

---

## 1. まず `_selftest.html` を開く

`_selftest.html` をブラウザで開き、先頭に **`21 / 21 PASS`** と出ることを確認。

- 全部緑なら、時刻選択ロジック（2時境界・平日土休日判定・直前1本＋次9本・時刻整形）が正しい
- 赤が1つでも出たら、その行の `got` と `want` を教えてください
- 何も表示されない／真っ白なら JS 構文エラーです。F12 のコンソール内容を教えてください

`index.html` に入っている4関数と `_selftest.html` の4関数が文字レベルで一致していることは自動確認済みなので、
ここが通れば「検証したコードがそのまま動いている」と言えます。

---

## 2. `index.html` を開いて既存機能が変わっていないか

追跡パネル以前の機能が改修前と同じであること。ここが最重要です。

- [ ] 時計が1秒ごとに進む
- [ ] 日付と曜日が正しい
- [ ] バッテリー表示（`⚡` と色分け）
- [ ] `↺` を押すとスピナーが回り、天気と電車情報が再取得される
- [ ] 天気モード: 傘行・2地点の今日/明日が出る。カウントダウンは出ない
- [ ] CDモード: カウントダウンのみ。天気・NTPが出ない
- [ ] モードセレクトが 天気 → CD → ALL → 天気 で循環する
- [ ] リロードしてもモードが復元される
- [ ] 任意時刻ボタンが CD/ALL でのみ出て、入力が反映される
- [ ] ランクサマリー行（`A / A / A / A- / C` など）が出る
- [ ] 広域カードと監視5路線カードが出る
- [ ] F12 コンソールにエラーが出ていない

---

## 3. 追跡パネルの表示条件

- [ ] 天気モードとCDモードでは追跡パネルが**一切出ない**
- [ ] ALLモードにすると、リンク行の下に `追跡 OFF` ボタンが出る
- [ ] `追跡 OFF` を押すと `追跡 ON` になり、方向トグルと駅ボタンが出る
- [ ] リロードしても ON/OFF が保持される
- [ ] ONのまま天気モードへ移り、ALLに戻すとONのまま出る

---

## 4. 方向と駅の初期選択

開いた時刻で初期値が変わります。

- [ ] 2:00〜11:59 に開くと `△行き` と `秦野` が選択済み
- [ ] 12:00〜翌1:59 に開くと `▼帰り` と `中央林間` が選択済み
- [ ] `△行き` の駅ボタン順が 秦野 / 相模大野 / 中央林間 / 南町田
- [ ] `▼帰り` の駅ボタン順が 南町田 / 中央林間 / 相模大野 / 秦野
- [ ] 方向を切り替えると、その方向の初期駅に戻る
- [ ] 8ボードすべて（4駅 × 2方向）ボタンで切り替わり、内容が変わる

---

## 5. 時刻表リストの見え方

`△行き` の `秦野` がサンプル12本入りなので、ここで見てください。

- [ ] 直前に出た1本が薄いグレーで表示される
- [ ] 次に来る列車に `▶` が付き、太字になる
- [ ] 次に来る列車の右端に残り時間が出て、**1秒ごとに減る**
- [ ] 行が「時刻 / 種別 / 行先 / 到着」の順に縦に揃っている
- [ ] リスト内をスクロールしても、1秒後に先頭へ戻されない
- [ ] 駅ボタンを押し替えると、スクロール位置が先頭に戻る

他のボードはサンプル1本だけなので、リストが1行でも異常ではありません。

---

## 6. スマホ幅

F12 → デバイスツールバー（Ctrl+Shift+M）で幅 375px にする。

- [ ] 横スクロールバーが**出ない**
- [ ] 時刻表の行が折り返さず、はみ出さない
- [ ] 駅ボタンが2段になっても崩れない

---

## 7. 遅延マーク（実際に遅れていないと出ません）

小田急小田原線・小田急江ノ島線・東急田園都市線のいずれかが平常以外のとき、
そのボードの全行に赤い `!` が付き、行がランク色になります。

- [ ] 平常時（ランクサマリーが全部 `A`）は `!` が出ていない

遅延中に確認できたら、`!` が付いた状態でも 375px で横スクロールが出ないかを見てください。
確認できないまま実データ投入に進んでも構いません（コードは検証済み）。

---

## 気になった点の伝え方

「どのモード・どの方向・どの駅・何時ごろ」を添えてもらえると特定が速くなります。
スクリーンショットがあれば確実です。
