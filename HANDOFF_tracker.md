# TrainWatcher 追跡パネル 引き継ぎ

作成: 2026-07-31
更新: 2026-07-31（Task 10 のエージェント停止後、停止時点の成果物を全量検証して追記）
状態: **Task 1〜10 の実装・検証すべて完了 / 残りはブラウザ目視確認とコミットのみ**

新セッションはこのファイルだけ読めば追いつけるようにしてある。

## 現在の到達点（2026-07-31 時点の最新）

Task 10 のエージェントはユーザー操作で停止したが、**停止前に埋め込み自体は完走していた。**
停止時点の成果物をサンプリングせず全量検証し、問題なしを確定済み。詳細は
`VERIFY_tracker_task10.md`（このファイルと同じフォルダ）。

- 実データ 3,185 本が `index.html` に埋め込み済み。`timetable.js` と文字レベル完全一致
- UTF-8(BOMなし)・CRLF 維持（素の LF は 0）
- baseline（`86bd92d`）比で **334行の純追加・0削除・0置換**。既存1,129行は無変更
- 追加CSS 22セレクタはすべて追跡パネル専用。既存レイアウトへの波及なし
- `arr == -1` の 305 件は**全件が正当**（下記「解決済みの懸念」参照）
- Task 9 の軽微指摘（`lineRankMap` とコメントの順序）は**適用済み**

**残っているのは以下だけ。**

1. ブラウザでの目視確認（`CHECK_tracker_2.md`）。JS構文エラーと描画はこれでしか分からない
2. `_selftest.html` の削除（目視確認1回目が未報告なので、確認が済むまで残してある）
3. コミット（コマンドは `CHECK_tracker_2.md` 末尾。実行はユーザー）

---

## スコープ

TrainWatcher（`index.html` 単体ダッシュボード）の ALL モードに「追跡パネル」を追加する。
通勤経路4駅について、現在時刻基準で「直前に出た1本＋次に来る9本」の時刻表を表示する。

| 種類 | パス |
|---|---|
| 本体 | `index.html` |
| 既存機能の仕様 | `SPEC.md` |
| 追跡パネル設計書（正本） | `SPEC_tracker.md` |
| 実装計画 | `PLAN_tracker.md` |
| 目視確認1回目 | `CHECK_tracker_1.md` |
| 作業台帳・レポート類 | scratchpad の `sdd_tracker/progress.md` |
| 取得スクリプト | scratchpad の `tw_build/` |

scratchpad の実パス:
`C:\Users\SHOTAT~1\AppData\Local\Temp\claude\C--Users-ShotaTanaka-Documents--------\7786d944-750b-4f47-bc1a-27177caa3590\scratchpad`

---

## 確定事項

### 運用ルール（この作業で合意したもの）

- **git のコミットは一切していない。** 変更は作業ツリー上にある。最後にまとめてコミットコマンドを提示する
- 差分レビューは `sdd_tracker/snapshots/` のスナップショット比較で行う
- **`git diff` はコミット `86bd92d`（42,639 bytes）との比較**になるので、単一タスクの差分を見たい時はスナップショットと比較する
- 目視確認はユーザーが2回だけ実施（Task 9 後・Task 10 後）
- `index.html` は **UTF-8(BOMなし)・CRLF** を維持。毎タスクでバイト単位検証済み

### 画面仕様

- ALL モード（コード内部名 `'both'`）限定。天気モード・CDモードでは表示しない
- `追跡 ON/OFF` は `localStorage['trainwatch_tracker_on']`（`'1'`/`'0'`、既定 OFF）で復元
- 方向・駅は保存しない。起動時刻で決定（2:00〜11:59 → 行き、それ以外 → 帰り）
- 既定駅: 行き `G1`（秦野）／帰り `K2`（中央林間）

### 8ボード

すべて発時刻ベース。到着列は乗換駅着、乗換がない G4/K4 は終着駅着。

| ID | 方向 | 駅 | 路線・方面 | Yahoo | 到着列 |
|---|---|---|---|---|---|
| G1 | 行き | 秦野 | 小田急小田原線 上り | 23283/3090 | 相模大野着 |
| G2 | 行き | 相模大野 | 小田急江ノ島線 下り | 23172/3111 | 中央林間着 |
| G3 | 行き | 中央林間 | 東急田園都市線 上り | 23231/3160 | 南町田着 |
| G4 | 行き | 南町田 | 東急田園都市線 上り | 22998/3160 | 終着着 |
| K1 | 帰り | 南町田 | 東急田園都市線 下り | 22998/3161 | 中央林間着 |
| K2 | 帰り | 中央林間 | 小田急江ノ島線 上り | 23231/3110 | 相模大野着 |
| K3 | 帰り | 相模大野 | 小田急小田原線 下り | 23172/3091 | 秦野着 |
| K4 | 帰り | 秦野 | 小田急小田原線 下り | 23283/3091 | 終着着 |

### データ仕様

- Yahoo URL: `/timetable/{駅ID}/{路線方面ID}?kind=1(平日)/2(土)/4(日祝)`
- 詳細ページ: `/timetable/{駅ID}/{路線方面ID}/{列車ID}?kind=N&hh=H&mm=M`
- **土曜と日祝はダイヤ完全一致を実データで確認済み** → 平日 `wd` / 土休日 `ho` の2区分で格納
- 1列車 = `[発分, 種別index, 行先index, 着分]`。着分不明は `-1`
- 0時台の列車は `1440 +` で当日末尾に格納
- 実行時は 2:00 を日境界とし、0:00〜1:59 は前日ダイヤを参照
- 祝日は 2026〜2027年分＋年末年始(12/29〜1/3)を埋め込み

### 実測した列車本数（2026-07-31 時点）

G1 261 / G2 358 / G3 496 / G4 496 / K1 504 / K2 335 / K3 468 / K4 267 = **3,185本**

### 方針変更（重要）

当初は短区間4ボード（G2/G3/K1/K2）を種別別の固定所要時間で算出するハイブリッド方式だった。
実測で **4ボードすべて所要時間が種別内で1分ばらつく**ことが判明したため、
**8ボードすべて詳細ページ実取得**に変更（ユーザー承認済み）。

- 追加コストは約17分のみ。データ件数は変わらないのでファイルサイズへの影響なし
- 中央林間・相模大野の乗り換え時間が3〜6分なので、1分の誤差は乗り継ぎ可否を反転させる
- 検証記録は `tw_build/out/calibration.md`

### パーサの設計判断

**読めないデータは黙って飛ばさず例外を投げる。** 約3,200ページに対して回すため、
1件の欠落がそのまま誤った時刻の表示になる。レビューで2回この種の指摘が出て両方修正済み。

- `parse_index`: `<dt>` に数字が無ければ `ValueError`。凡例の解析漏れは `legend_skipped` に記録
- `parse_detail`: セレクタを `ul.elmStopSta div.station` に限定。
  駅名なし・時刻なし・親要素なしはすべて `ValueError`
- 日跨ぎ補正は `ROLLOVER_MIN_JUMP = 720` を超える巻き戻りのみ。それ未満は `ValueError`
  （1分の乱れを日跨ぎと誤認すると以降の全駅が24時間ずれる）
- 凡例（種別・行先の記号）は**ページから読む**。ハードコードしない（路線ごとに記号が違う）

### 遅延マーク

- `refresh()` が `lineRankMap = {路線名: {rank, color}}` を5分ごとに更新
- `trackerDelayInfo(lineName)` はランク `A` と `-` で `null`。`A-`〜`E` で `{rank,color}`
- `-` は「取得失敗」。ここで赤くすると単なる通信失敗を路線障害と誤読させる
- キーは `TARGET_LINES` の路線名と完全一致が必須（1文字違うとエラーも出ずマークが出ない）
- `delay.color` は `calculateRank` のハードコード hex のみ。スクレイプ文字列は到達しない（レビューで追跡確認済み）

---

## 解決済みの懸念

### 「到着なしが1%超ならパーサ問題」という判定基準は誤りだった

`PLAN_tracker.md` は `arr == -1` が1%を超えたらパーサ問題と定めていたが、
実測は 305/3,185 = **9.6%**。この閾値は
**到着駅より手前で終着する列車・到着駅を通過する列車の存在を考慮していなかった。**

305件すべてをキャッシュから再パースして検証した結果、
**到着駅が停車駅リストに存在するのに拾えていないケースは0件**。
理由は次の2つだけで、いずれも `ride_minutes()` が `None` を返すのが正しい。

- **手前で終着**: G1/K3 の本厚木・伊勢原・海老名行き（K3 は 236本中126本がこれ）
- **通過**: 特急ロマンスカー（G1の新宿行きは相模大野を通過し町田に停車、G2は中央林間を通過）

内訳の全件表は `VERIFY_tracker_task10.md` セクション4。
**再取得しても同じ比率が出る。パーサを疑わないこと。**

---

## 未確定・未確認

- **`_selftest.html` の `21 / 21 PASS` がブラウザ未確認。**
  同じ21アサーションは `tw_build/tests/test_tracker_logic.py` で機械検証済み（アルゴリズムは確定）。
  ブラウザ確認でしか分からないのは JS 構文エラーと実際の描画
- 目視確認1回目（`CHECK_tracker_1.md`）の結果が未報告
- 遅延マークの実表示は、実際に路線が遅延していないと確認できない
- 2028年以降の祝日はリスト外（平日扱いになる）。データ再取得時に更新する運用

---

## 次の作業

### 完了済み（2026-07-31）

- ~~1. `build.py` の完走~~ → 完走。cache 3,202ファイル、16区分すべて件数一致
- ~~2. `verify.py` 実行と `arr == -1` の件数確認~~ → 305件を全量検証、全件正当（上記「解決済みの懸念」）
- ~~3. Task 10 のデータ埋め込み~~ → 完了。`timetable.js` と文字レベル一致、CRLF/BOMなし維持
- ~~4. Task 9 の軽微指摘（`lineRankMap` とコメントの順序）~~ → 適用済み
- ~~6. 既存機能のデグレ点検（静的）~~ → 334行純追加・0削除・0置換、CSSスコープ限定を確認

### 残り

1. **ユーザーがブラウザで目視確認**（`CHECK_tracker_2.md`）。
   JS構文エラーと実際の描画はこの環境（node/bun/deno なし）では確認できない
2. `_selftest.html` を削除。
   ただし**目視確認1回目が未報告**なので、`21 / 21 PASS` を見るまでは残しておく
3. コミット（コマンドは `CHECK_tracker_2.md` 末尾。実行はユーザー・ルール2）
4. 未決の設計判断1件: 到着駅に行かない列車（305件）を出したままにするか。
   現状は仕様どおり「出す」で実装済み。`VERIFY_tracker_task10.md` セクション7

---

## 踏んだ落とし穴

- `Path.read_text()`/`write_text()` は Python 3.11 では `newline=` を受け付けない
- Windows コンソールは cp932。日本語や em-dash の `print()` で `UnicodeEncodeError`。
  `PYTHONIOENCODING=utf-8` を付ける
- bash heredoc で `.py` を書くと日本語が化けてパースできない。Write ツールを使う
- node/bun/deno が無いので JS を実行できない
- pytest 未インストール。`python -m unittest` を使う
- Yahoo のページは UTF-8。`res.text` ではなく `res.content.decode('utf-8')`
- `<dt>` に `●`（当駅始発マーク）が混入する
- 駅名に `海老名(相鉄・小田急)` のような注記が付く
- 秦野上り平日は **131本**。WebFetch が「150本」と答えたのは集計ミス
