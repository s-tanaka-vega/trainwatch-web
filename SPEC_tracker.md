# TrainWatcher 追跡パネル 設計書

作成日: 2026-07-31
対象: `work/OLDproject/TrainWatch/index.html`（単体HTML）
関連: `SPEC.md`（既存機能の仕様）

---

## 1. 目的とスコープ

ALL モードに「追跡パネル」を追加する。通勤経路上の4駅について、
現在時刻を基準に「直前に出た1本＋次に来る9本」の時刻表を表示する。

### やること

- ALL モード限定の追跡パネル（ON/OFF ボタン、方向トグル、駅ボタン、時刻表リスト）
- 時刻表データを Yahoo!路線情報から実取得し、`index.html` に静的埋め込み
- 既存の運行情報ランクに連動した遅延マーク表示

### やらないこと

- リアルタイム時刻更新（ダイヤ改正時は再取得してデータを差し替える運用）
- 天気モード・CD モードでの表示（ALL モードのみ）
- 既存機能（天気・カウントダウン・電車カード・ランクサマリー）の挙動変更
- 「表示の出し分けを全要素に拡張」する改修（別件として後日）

---

## 2. 画面仕様

### 2.1 配置

既存のリンク行（`#weather-links`）の**直下**、電車カード（`#cards`）の**上**に挿入する。

```
┌──────────────────────────────────┐
│ [TrainWatcher] [日付] [🔋XX%]  ↺ │
├──────────────────────────────────┤
│   09:43:21                       │
│ ─────────────                    │
│   [カウントダウン・メイン]        │
│   [カウントダウン・サブ]          │
│   [NTP] [天気行]                  │
│   A / A / A / A- / C / 3          │
│ 秦野天気 / 関東広域電車情報 /     │
│   モードセレクト(ALL) / 任意時刻  │
│ ┌────────── 追跡パネル ────────┐ │  ← 新規
│ │ [ 追跡 ON ]                  │ │
│ │  △行き    ▼帰り              │ │
│ │ [秦野][相模大野][中央林間]   │ │
│ │ [南町田]                     │ │
│ │ ┌── スクロール領域 ──────┐   │ │
│ │ │  08:04 快急 新宿 大野08:28│  │ │
│ │ │▶ 08:13 急行 新宿 大野08:41│  │ │
│ │ │  08:20 急行 町田 大野08:47│  │ │
│ │ │  ...（計10行）            │  │ │
│ │ └──────────────────────┘   │ │
│ └──────────────────────────────┘ │
├──────────────────────────────────┤
│ 広域カード                        │
│ 監視路線カード × N                │
└──────────────────────────────────┘
```

### 2.2 追跡 ON/OFF ボタン

- 常にリンク行の下に1個表示（ALL モード時のみ）
- OFF のとき: ボタンのみ表示。方向トグル・駅ボタン・時刻表は非表示
- ON のとき: 全要素を表示
- 状態を `localStorage['trainwatch_tracker_on']` に `'1'`/`'0'` で保存し、次回起動時に復元
- 初期値（保存なし）: OFF

### 2.3 方向トグル

- `△行き` / `▼帰り` の2択。選択中は文字色を強調、非選択はグレー
- 初期選択は起動時刻で自動決定
  - 2:00〜11:59 → 行き
  - 12:00〜翌1:59 → 帰り
- 保存しない（起動のたびに時刻ルールで決まる）

### 2.4 駅ボタン

- `秦野` `相模大野` `中央林間` `南町田` の4個
- 方向を切り替えても4個とも表示する（並び順のみ経路順に入れ替える）
  - 行き: 秦野 → 相模大野 → 中央林間 → 南町田
  - 帰り: 南町田 → 中央林間 → 相模大野 → 秦野
- 初期選択
  - 行き → `秦野`
  - 帰り → `中央林間`
- 方向を切り替えたら、その方向の初期駅に戻す
- 保存しない

### 2.5 時刻表リスト

- 現在時刻基準で、**直前に出た1本＋次に来る9本＝計10行**
- スクロール領域（`max-height: 260px; overflow-y: auto`）に入れ、初期スクロール位置は先頭（直前1本が見える位置）
- 1行の構成（左から固定幅カラム）

  | カラム | 幅 | 内容 |
  |---|---|---|
  | マーカー | 1em | 次発のみ `▶`、それ以外は空白 |
  | 発時刻 | 3.4em | `08:13` |
  | 遅延 | 0.8em | 遅延時のみ `!`（赤） |
  | 種別 | 2.6em | `各停`/`準急`/`急行`/`快急`/`特急`/`区急` |
  | 行先 | 4.5em | `新宿` 等。あふれたら省略記号 |
  | 到着 | 残り | `大野 08:41` のように略駅名＋時刻 |
  | 残り時間 | 3.6em | 次発の行のみ `3:21` |

- 直前1本の行は `opacity: 0.4` でグレーアウト
- 次発の行は `font-weight: 700`
- 1秒ごとに残り時間を再計算し、次発が過ぎたらリストを1行ぶんスライドさせる

### 2.6 遅延マーク

- 判定対象は既存の `calculateRank()` の結果（`refresh()` が5分ごとに更新）
- 選択中ボードの路線ランクが **`A` 以外**のとき、そのボードの**全行**に赤い `!` を表示し、
  発時刻・種別・行先の文字色をランク色（`A-`=#A8D18D / `B`=#FBBC04 / `C`=#EA4335 / `D`=#FF6D01 / `E`=#B21111）にする
- 実際にその列車が遅れているかは問わない（路線単位の表示）
- ランクが取得できていない（初回ロード中・取得失敗）の場合はマークなし

### 2.7 スマホ対応

- 既存の `body { max-width: 640px }` の内側に収める
- 時刻表の基準フォントサイズ `0.82rem`、行高 `1.9em`
- カラム幅は `em` 指定＋`white-space: nowrap` で折り返さない
- 駅ボタンは `flex-wrap: wrap` で2段になっても崩れないようにする

---

## 3. データ仕様

### 3.1 ボード定義（8種）

| ID | 方向 | 駅 | 路線・方面 | Yahoo URL | 到着列 | 到着取得方法 |
|---|---|---|---|---|---|---|
| G1 | 行き | 秦野 | 小田急小田原線 上り(新宿方面) | `/timetable/23283/3090` | 相模大野着 | 詳細ページ実取得 |
| G2 | 行き | 相模大野 | 小田急江ノ島線 下り(藤沢方面) | `/timetable/23172/3111` | 中央林間着 | 詳細ページ実取得 |
| G3 | 行き | 中央林間 | 東急田園都市線 上り(押上方面) | `/timetable/23231/3160` | 南町田着 | 詳細ページ実取得 |
| G4 | 行き | 南町田 | 東急田園都市線 上り(押上方面) | `/timetable/22998/3160` | 終着駅着 | 詳細ページ実取得 |
| K1 | 帰り | 南町田 | 東急田園都市線 下り(中央林間方面) | `/timetable/22998/3161` | 中央林間着 | 詳細ページ実取得 |
| K2 | 帰り | 中央林間 | 小田急江ノ島線 上り(相模大野方面) | `/timetable/23231/3110` | 相模大野着 | 詳細ページ実取得 |
| K3 | 帰り | 相模大野 | 小田急小田原線 下り(小田原方面) | `/timetable/23172/3091` | 秦野着 | 詳細ページ実取得 |
| K4 | 帰り | 秦野 | 小田急小田原線 下り(小田原方面) | `/timetable/23283/3091` | 終着駅着 | 詳細ページ実取得 |

**2026-07-31 変更**: 当初は短区間4ボード（G2/G3/K1/K2）を種別別の固定所要時間で算出する
ハイブリッド方式としていたが、実測（`tw_build/out/calibration.md`）で4ボードすべて
所要時間が種別内で1分ばらつくことが判明したため、**8ボードすべて詳細ページ実取得**に変更した。
追加コストは約17分のみで、データ件数は変わらないためファイルサイズへの影響はない。

### 3.2 遅延マークの路線紐付け

| ボード | 参照する TARGET_LINES 名 |
|---|---|
| G1 / K3 / K4 | `小田急小田原線` |
| G2 / K2 | `小田急江ノ島線` |
| G3 / G4 / K1 | `東急田園都市線` |

3路線とも既存の `TARGET_LINES` に含まれているため、追加の取得は不要。

### 3.3 ダイヤ区分

- Yahoo の `kind` パラメータ: `1`=平日 / `2`=土曜 / `4`=日曜祝日
- 取得時に3区分すべて取得し、`kind=2` と `kind=4` が完全一致なら**平日 / 土休日の2区分**に畳んで格納する
- 一致しない場合は**平日 / 土曜 / 日祝の3区分**で格納する（データ量は約1.5倍）
- 実行時の区分判定
  - 祝日リスト（後述）に該当 → 日祝（2区分時は土休日）
  - 土曜 → 土曜（2区分時は土休日）
  - 日曜 → 日祝（2区分時は土休日）
  - それ以外 → 平日

### 3.4 祝日リスト

- `index.html` に 2026年・2027年の日本の祝日（振替休日含む）を `['2026-01-01', ...]` 形式の配列で埋め込む
- 年末年始（12/29〜1/3）は鉄道各社が土休日ダイヤで運行するため、祝日扱いとしてリストに含める
- 2028年以降はリスト外となり平日扱いになる。データ再取得時に更新する運用とする

### 3.5 埋め込みデータ構造

```js
// 取得日。表示はしないが再取得時期の判断用にコメントとして残す
const TT_FETCHED = '2026-07-31';

// 種別コード（index 参照）
const TT_TYPES = ['各停','準急','急行','快急','特急','区急'];

// 行先の文字列プール（index 参照）
const TT_DESTS = ['新宿','町田','相模大野','本厚木','小田原', /* ... */];

// ボードごとの時刻表
// 1列車 = [発分, 種別index, 行先index, 着分]
//   発分/着分 = その運行日の 0:00 からの経過分。
//   0時台以降の列車は 1440 を加算して格納する（例 0:14発 → 1454）
//   着分が日を跨ぐ場合も同様に 1440 を加算する
//   着分が不明な列車は -1
const TIMETABLE = {
  G1: { line:'小田急小田原線', arrLabel:'大野',
        wd: [[484,3,0,512], /* ... */],
        ho: [/* ... */] },
  // G2 ... K4
};
```

- 想定データ量: 8ボード × 2区分 × 約150〜250本 ≒ 2,800行 → 約60〜80KB
- `index.html` は現在 42KB。追加後は約 110〜130KB を見込む

### 3.6 時刻の基準（2:00 境界）

行き／帰りの初期判定と同じく、**2:00 を1日の境界**として扱う。

- 現在時刻 `H:M` に対し `nowMin = H*60 + M`
- `nowMin < 120`（0:00〜1:59）のとき: `nowMin += 1440` とし、**前日**の曜日でダイヤ区分を判定する
- これにより 0時台の列車（1440以上で格納）と正しく比較できる

---

## 4. データ取得スクリプト

### 4.1 方針

- `index.html` とは別の Python スクリプトとして作成し、scratchpad に置く
- 出力は JS の `const` 宣言を含むテキストファイル。`index.html` へは生成結果のみを貼り込む
- スクリプト自体は成果物に含めない（配布は `index.html` 単体のまま）

### 4.2 取得量

実測した列車本数（2026-07-31 時点、平日 + 土休日）。

| ボード | 駅 / 路線 | 平日 | 土休日 | 計 |
|---|---|---|---|---|
| G1 | 秦野 / 小田原線 上り | 131 | 130 | 261 |
| G2 | 相模大野 / 江ノ島線 下り | 179 | 179 | 358 |
| G3 | 中央林間 / 田園都市線 上り | 258 | 238 | 496 |
| G4 | 南町田 / 田園都市線 上り | 258 | 238 | 496 |
| K1 | 南町田 / 田園都市線 下り | 261 | 243 | 504 |
| K2 | 中央林間 / 江ノ島線 上り | 171 | 164 | 335 |
| K3 | 相模大野 / 小田原線 下り | 236 | 232 | 468 |
| K4 | 秦野 / 小田原線 下り | 131 | 136 | 267 |
| 合計 | | 1,625 | 1,560 | **3,185** |

| 種別 | 件数 |
|---|---|
| 時刻表インデックス | 8ボード × 3 kind = 24ページ |
| 列車詳細 | 3,185ページ |
| 合計 | 約3,209リクエスト |

- リクエスト間隔 0.6秒、直列実行。所要約32分
- 失敗時は3回までリトライ。3回失敗した列車は着分 `-1` として記録し、ログに残す
- 取得済みレスポンスはローカルにキャッシュし、再実行時は再取得しない

### 4.3 固定所要時間の検証結果（採用せず）

短区間4ボードについて種別ごとに5本ずつサンプル取得し（73リクエスト）、所要時間の一定性を検証した。
結果は4ボードすべて「種別内で1分ばらつく」であり、固定所要時間は採用できないと判断した。

| ボード | 一定だった種別 | ばらついた種別 |
|---|---|---|
| G2 相模大野→中央林間 | 各停4分 / 快急3分 / 急行3分 | （特急はサンプルなし） |
| G3 中央林間→南町田 | 各停3分 | 急行 2〜3分 / 準急 3〜4分 |
| K1 南町田→中央林間 | なし | 各停 4〜5分 / 急行 3〜4分 / 準急 4〜5分 |
| K2 中央林間→相模大野 | 快急4分 | 各停 4〜5分 / 急行 3〜4分 |

中央林間・相模大野の乗り換え時間が3〜6分であり、1分の誤差は乗り継ぎの可否を反転させうる。
このため全区間を実取得する方針に変更した（ユーザー承認済み）。

検証スクリプト `calibrate.py` と結果 `out/calibration.md` は判断の記録として残す。

### 4.4 検証

- 出力後、8ボード × 区分ごとに本数・始発・終電を Yahoo のページ表示と突き合わせる
- 到着時刻が発時刻より前になっている行がないか全件チェックする
- 着分 `-1` の件数を集計して報告する

---

## 5. 実装方針（デグレ対策）

既存挙動を一切変えないことを最優先とする。

- 既存関数は書き換えない。`applyDisplayMode()` にのみ**1ブロック追加**する
  （ALL モードのとき追跡パネルのラッパーを表示、それ以外は非表示）
- 追跡パネルの DOM は既存要素と ID が衝突しない `tracker-` 接頭辞で新規追加する
- 追跡パネルの JS は独立した関数群（`initTracker` / `renderTracker` / `pickTrains` 等）にまとめ、
  既存のグローバル変数を書き換えない
- 遅延ランクの参照は、`refresh()` の結果を格納する新しいグローバル
  `lineRankMap`（`{路線名: {rank, color}}`）を1つ追加して読み取る。
  `refresh()` への追加は「このマップへの代入1行」のみ
- 1秒ごとの再描画は既存の `setInterval(updateCountdown, 1000)` とは**別**の interval を立てる
  （既存タイマーの中身を触らない）
- 追跡 OFF 時は再描画処理を即 return させ、無駄な処理を発生させない
- 文字コード UTF-8(BOMなし)・改行 CRLF を維持する。編集後にバイト単位で検証する

---

## 6. 受け入れ条件

- [ ] ALL モード以外では追跡パネルが一切表示されない
- [ ] 追跡 ON/OFF がリロード後も復元される
- [ ] 起動時刻 2:00〜11:59 で「行き・秦野」、12:00〜1:59 で「帰り・中央林間」が選ばれる
- [ ] 4駅 × 2方向の全8ボードが表示でき、発時刻・種別・行先・到着時刻が出る
- [ ] 直前1本がグレーアウトで表示され、次発に `▶` と残り時間が出る
- [ ] 平日・土休日がカレンダーに応じて切り替わる（祝日リスト該当日を含む）
- [ ] 0時台の列車が当日の末尾に正しく並び、0時台に開いても比較が破綻しない
- [ ] 該当路線のランクが A 以外のとき、全行に赤い `!` とランク色が付く
- [ ] 幅375px（iPhone 相当）で横スクロールが発生しない
- [ ] 天気・カウントダウン・電車カード・ランクサマリーの表示が改修前と同一
- [ ] `index.html` が UTF-8(BOMなし)・CRLF のまま
