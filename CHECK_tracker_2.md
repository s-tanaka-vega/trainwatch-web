# 追跡パネル 目視確認 2回目（実データ入り・最終）

日付: 2026-07-31
対象: `index.html`（実データ 3,185 本 埋め込み後）
関連: `VERIFY_tracker_task10.md` / `HANDOFF_tracker.md` / `SPEC_tracker.md`

コード・データ・デグレの**静的検証はすべて完了**している（`VERIFY_tracker_task10.md`）。
この環境に node/bun/deno が無いため、**JS構文エラーと実際の描画だけ**が未確認。
それを潰すのがこの確認の目的。

今日 2026-07-31 は**金曜**なので、表示されるのは平日ダイヤ（`wd`）。

---

## 0. まず最優先の1点

`index.html` をブラウザで開いて、**画面が真っ白でないこと**を確認。

真っ白なら JS 構文エラーです。F12 → Console の**1行目のエラー文と行番号**を教えてください。
埋め込んだデータは 232 行あるので、そこが原因なら行番号でほぼ特定できます。

ここが通れば構文は無事です。

---

## 1. 既存機能が変わっていないか（最重要）

静的には既存1,129行が無変更（0削除・0置換）と確認済みですが、実行時の確認をお願いします。

- [ ] 時計が1秒ごとに進む
- [ ] 日付と曜日が正しい
- [ ] バッテリー表示（`⚡` と色分け）
- [ ] `↺` を押すとスピナーが回り、天気と電車情報が再取得される
- [ ] 天気モード: 傘行・2地点の今日/明日が出る。カウントダウンは出ない
- [ ] CDモード: カウントダウンのみ。天気・NTPが出ない
- [ ] モードセレクトが 天気 → CD → ALL → 天気 で循環する
- [ ] リロードしてもモードが復元される
- [ ] 任意時刻ボタンが CD/ALL でのみ出て、入力が反映される
- [ ] ランクサマリー行（`A / A / A / A- / C` など）が出る
- [ ] 広域カードと監視5路線カードが出る
- [ ] 温度色・NTPのOK/NG表示が従来どおり
- [ ] F12 コンソールにエラーが出ていない

---

## 2. 実データの妥当性（1回目と違い、ここが本番）

`追跡 ON` にして、各ボードの**始発と終電**が下表と合っているか見てください。
リストは現在時刻基準（直前1本＋次9本）なので、始発・終電を見るには
**任意時刻ボタンで 4:50 や 0:20 を入れる**のが早いです。

| 方向 | 駅ボタン | 平日の本数 | 始発 | 終電 |
|---|---|---|---|---|
| △行き | 秦野 | 131 | 05:06 | 00:14 |
| △行き | 相模大野 | 179 | 04:55 | 00:30 |
| △行き | 中央林間 | 258 | 05:05 | 00:32 |
| △行き | 南町田 | 258 | 05:08 | 00:36 |
| ▼帰り | 南町田 | 261 | 05:05 | 00:46 |
| ▼帰り | 中央林間 | 171 | 05:17 | 00:55 |
| ▼帰り | 相模大野 | 236 | 04:53 | 00:43 |
| ▼帰り | 秦野 | 131 | 05:27 | 00:29 |

- [ ] 8ボードすべて始発・終電が上表と一致する
- [ ] 種別（各停・準急・急行・快急・特急）が実際のダイヤと合っている
- [ ] 行先が実際のダイヤと合っている
- [ ] 到着時刻が乗換に使える値になっている（下記参照）

### 乗り継ぎの実用チェック（ここが本命）

通勤で実際に使う組み合わせを1つ見てください。

- [ ] `△行き / 秦野` の相模大野着と、`△行き / 相模大野` の発時刻を見比べて、
      **乗り継ぎ時間が3〜6分程度**になっている（Task 4 で1分精度が必要と判明した箇所）
- [ ] 同様に `△行き / 相模大野` の中央林間着 → `△行き / 中央林間` の発

---

## 3. 到着が `--:--` の行（仕様確認込み）

実データでは **305本（全体の9.6%）が到着列 `--:--`** になります。**これは正常です。**

理由は2つだけで、全件検証済み（`VERIFY_tracker_task10.md` セクション4）。

- 到着駅より**手前で終着**する列車（本厚木・伊勢原・海老名行き）
- 到着駅を**通過**する特急ロマンスカー

とくに目立つ場所:

- [ ] `▼帰り / 相模大野` は 236本中126本が `--:--`（本厚木・伊勢原止まり）。**半分以上が `--:--` で正常**
- [ ] `△行き / 秦野` の「特急 新宿行き」が `--:--`（相模大野を通過し町田に停車するため）
- [ ] `△行き / 相模大野` の「特急 藤沢・片瀬江ノ島行き」が `--:--`（中央林間を通過）
- [ ] `▼帰り / 南町田`・`▼帰り / 中央林間`・`▼帰り / 秦野`・`△行き / 中央林間`・`△行き / 南町田`
      には `--:--` が**1本も無い**

**ここが仕様判断のポイント:**
到着駅に行かない列車を今のように**出したまま**にするか、隠す／灰色にするか。
現状は `SPEC_tracker.md` の設計どおり「出す」です。変えたい場合は指示をください。

---

## 4. 表示条件・初期選択（1回目と同じ観点の再確認）

- [ ] 天気モードとCDモードでは追跡パネルが**一切出ない**
- [ ] ALLモードで、リンク行の下に `追跡 OFF` ボタンが出る
- [ ] `追跡 OFF` → `追跡 ON` で方向トグルと駅ボタンが出る
- [ ] リロードしても ON/OFF が保持される
- [ ] ONのまま天気モードへ移り、ALLに戻すとONのまま出る
- [ ] 12:00〜翌1:59 に開くと `▼帰り` と `中央林間` が選択済み
- [ ] 2:00〜11:59 に開くと `△行き` と `秦野` が選択済み
- [ ] 方向を切り替えると、その方向の初期駅に戻る

---

## 5. リストの見え方（実データなので本数が多い）

- [ ] 直前に出た1本が薄いグレー、次に来る列車に `▶` と太字
- [ ] 次に来る列車の残り時間が**1秒ごとに減る**
- [ ] 行が「時刻 / 種別 / 行先 / 到着」で縦に揃っている
- [ ] リスト内をスクロールしても、1秒後に先頭へ戻されない
- [ ] 駅ボタンを押し替えると、スクロール位置が先頭に戻る
- [ ] **深夜0時台**（任意時刻に 00:10 等）で、0時台の列車が正しく末尾側に出る
- [ ] **1:00〜1:59**（任意時刻に 01:30）で、前日ダイヤ扱いになり終電付近が出る
- [ ] 始発前（任意時刻 04:00 等）でも壊れない

---

## 6. スマホ幅

F12 → デバイスツールバー（Ctrl+Shift+M）で幅 375px。

- [ ] 横スクロールバーが**出ない**
- [ ] 時刻表の行が折り返さず、はみ出さない
- [ ] 駅ボタンが2段になっても崩れない
- [ ] 到着が `--:--` の行でも崩れない

---

## 7. 遅延マーク（遅延していないと出ません）

- [ ] 平常時（ランクサマリーが全部 `A`）は `!` が出ていない

遅延中に見られたら、`!` 付きの状態で 375px の横スクロールが出ないかも見てください。
確認できないままコミットして構いません（ロジックは検証済み）。

---

## 8. 確認後のコミット（実行はユーザー）

問題がなければ以下。`_selftest.html` は**意図的に除外**しています
（確認用の一時ファイルなので、`21 / 21 PASS` を見たら削除してください）。

```
cd C:\Users\ShotaTanaka\Documents\work\OLDproject\TrainWatch
git add index.html SPEC_tracker.md PLAN_tracker.md CHECK_tracker_1.md CHECK_tracker_2.md HANDOFF_tracker.md VERIFY_tracker_task10.md
git status
git commit -m "TrainWatcher: ALLモードに通勤4駅の追跡パネルを追加（時刻表3185本を埋め込み）"
```

`git status` で `_selftest.html` だけが untracked に残るのが正しい状態です。

削除する場合:

```
del "C:\Users\ShotaTanaka\Documents\work\OLDproject\TrainWatch\_selftest.html"
```

---

## 気になった点の伝え方

「どのモード・どの方向・どの駅・何時ごろ」を添えてもらえると特定が速くなります。
スクリーンショットがあれば確実です。

時刻がずれている場合は、**その1本の発時刻と、あなたが正しいと思う到着時刻**を教えてください。
`tw_build/cache` に取得元HTMLが残っているので、再取得なしで突き合わせできます。
