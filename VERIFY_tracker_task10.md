# Task 10 実データ埋め込み 検証レポート

日付: 2026-07-31
対象: `index.html`（実データ埋め込み後）
前提資料: `SPEC_tracker.md` / `PLAN_tracker.md` / `HANDOFF_tracker.md`

Task 10 のエージェントがユーザー操作で停止したため、
停止時点の成果物を**サンプリングせず全量**検証した記録。

---

## 1. 結論

- **埋め込みは正常に完了している。** データ・エンコード・既存機能への影響、いずれも問題なし
- `PLAN_tracker.md` が置いた「到着なしが1%超ならパーサ問題」という閾値は**前提が誤り**だった。
  実測 9.6%（305/3,185件）だが、**全件が正当**であることを機械検証で確定
- 残る未検証項目は **JS構文エラーと実際の描画のみ**。
  この環境に node/bun/deno が無いため、ブラウザでしか確認できない

---

## 2. 埋め込み整合性（裏取り済み）

| 検証項目 | 結果 |
|---|---|
| `timetable.js` 本体と `index.html` 埋め込み部の一致 | 文字レベルで完全一致（49,787 文字） |
| `// TEMP: Task 10` ブロックの残存 | なし（0件） |
| `const TIMETABLE` の定義数 | 1（重複定義なし） |
| `index.html` バイト数 | 102,732 bytes（baseline 42,639 → +60,093） |

### エンコード・改行（ルール3）

| 項目 | 結果 |
|---|---|
| BOM | なし（維持） |
| UTF-8 デコード | OK |
| CRLF | 1,462 箇所 |
| 素の LF（CRLF でない `\n`） | **0**（混在なし） |

`embed.py` が「いったん LF に正規化してから CRLF 化」しているため、
Windows の `write_text` による二重変換は起きていない。

---

## 3. データ件数と突き合わせ

全 3,185 本。16区分（8ボード × wd/ho）すべてで
**インデックスページ再パース結果と `data.json` の件数・発時刻が完全一致**（不整合 0件）。

| ボード | 駅・方面 | wd | ho |
|---|---|---|---|
| G1 | 秦野 / 小田原線 上り | 131 | 130 |
| G2 | 相模大野 / 江ノ島線 下り | 179 | 179 |
| G3 | 中央林間 / 田園都市線 上り | 258 | 238 |
| G4 | 南町田 / 田園都市線 上り | 258 | 238 |
| K1 | 南町田 / 田園都市線 下り | 261 | 243 |
| K2 | 中央林間 / 江ノ島線 上り | 171 | 164 |
| K3 | 相模大野 / 小田原線 下り | 236 | 232 |
| K4 | 秦野 / 小田原線 下り | 131 | 136 |

cache は 3,202 ファイル（詳細 3,185 + インデックス等 17）。
`build.py` は 1件でも読めなければ例外を投げる設計なので、完走＝全件パース成功を意味する。

---

## 4. 到着時刻なし 305件の全量検証（最重要）

`arr == -1` の 305 件すべてについて、キャッシュから詳細ページを再パースし
**到着駅が停車駅リストに本当に存在しないか**を確認した。

### 判定

- **到着駅が停車駅リストに存在するのに拾えていないケース: 0件**
- したがって 305 件すべて、`ride_minutes()` が `None` を返すのが正しい挙動

### 内訳（全件）

理由は2種類しかない。

**(a) 到着駅より手前で終着する列車**

| ボード | 区分 | 種別 | 行先（＝終着） | 件数 |
|---|---|---|---|---|
| G1 | wd | 各駅停車 | 本厚木 | 5 |
| G1 | ho | 各駅停車 | 本厚木 | 8 |
| G1 | ho | 急行 | 本厚木 | 2 |
| K3 | wd | 各駅停車 | 本厚木 | 98 |
| K3 | wd | 各駅停車 | 伊勢原 | 12 |
| K3 | wd | 各駅停車 | 海老名(相鉄・小田急) | 1 |
| K3 | wd | 急行 | 本厚木 | 2 |
| K3 | wd | 急行 | 伊勢原 | 12 |
| K3 | wd | 準急 | 本厚木 | 1 |
| K3 | ho | 各駅停車 | 本厚木 | 86 |
| K3 | ho | 各駅停車 | 伊勢原 | 23 |
| K3 | ho | 各駅停車 | 海老名(相鉄・小田急) | 1 |
| K3 | ho | 急行 | 本厚木 | 1 |
| K3 | ho | 急行 | 伊勢原 | 6 |

**(b) 到着駅を通過する列車（特急ロマンスカー）**

| ボード | 区分 | 種別 | 行先 | 通過する到着駅 | 件数 |
|---|---|---|---|---|---|
| G1 | wd | 特急 | 新宿 | 相模大野（町田に停車） | 14 |
| G1 | ho | 特急 | 新宿 | 相模大野（町田に停車） | 11 |
| G2 | wd | 特急 | 片瀬江ノ島 | 中央林間 | 3 |
| G2 | wd | 特急 | 藤沢 | 中央林間 | 4 |
| G2 | ho | 特急 | 片瀬江ノ島 | 中央林間 | 12 |
| G2 | ho | 特急 | 藤沢 | 中央林間 | 1 |
| K3 | ho | 特急 | 箱根湯本 | 秦野 | 2 |

合計 305 件。ボード別内訳は G1 19+21 / G2 7+13 / K3 126+119 で、
`out/verify.md` の集計と一致する。G3・G4・K1・K2・K4 は 0 件。

### なぜ K3 が半分以上なのか

K3（相模大野発 小田原線 下り）は 236 本中 126 本が本厚木・伊勢原止まりで、
秦野まで行かない。これはダイヤの実態であり不具合ではない。

---

## 5. 既存機能へのデグレ静的検証（ルール9・11）

baseline（コミット `86bd92d`、1,129行）と現行（1,463行）を行単位で比較。

| 種別 | 件数 |
|---|---|
| 追加行 | 334 |
| **削除行** | **0** |
| **置換ブロック** | **0** |

既存 1,129 行が**1行も変更・削除されずそのまま残っている**。
よって既存機能が壊れる経路は「新規コードが実行時に干渉する」場合のみに限定される。

### 挿入8ブロックの評価

| # | 位置 | 行数 | 内容 | 干渉リスク |
|---|---|---|---|---|
| 1 | `<style>` 末尾 | 86 | 追跡パネル用CSS | なし（下記参照） |
| 2 | `#weather-links` の後・`#cards` の前 | 9 | `#tracker-wrap` を**兄弟として**追加 | なし（入れ子変化なし） |
| 3 | トップレベル | 3 | `let lineRankMap = {}` 宣言 | なし（新規変数） |
| 4 | `refresh()` 内 | 1 | `lineRankMap` へ代入 | なし（`lineItems` を読むだけ） |
| 5 | `applyDisplayMode()` 内 | 1 | `applyTrackerVisibility()` 呼び出し | 追跡パネル要素のみ操作 |
| 6 | `fmtRemain` の後 | 232 | 時刻表データ＋追跡パネル関数群 | なし（新規） |
| 7 | 起動処理 | 1 | 1秒間隔の `renderTrackerList` | `trackerOn && mode==='both'` でガード |
| 8 | 起動処理 | 1 | `initTracker()` 呼び出し | 新規初期化のみ |

ブロック2について、`git diff` 上は `</div>` の挿入に見えるが
これは difflib の整列アーティファクトで、実際の DOM は
`#weather-links` が baseline と同じ位置で閉じ、`#tracker-wrap` が
`#cards` の直前に兄弟として入っているだけ。**既存の入れ子構造は変わっていない。**

### CSS のスコープ

挿入した 86 行のセレクタは 22 個。すべて
`#tracker-wrap` / `#tracker-toggle` / `#tracker-body` / `#tracker-dir` / `#tracker-stations` /
`#tracker-list` / `#tracker-empty` / `.tracker-dir-btn` / `.tracker-st-btn` / `.tracker-row` /
`.tr-mark` / `.tr-dep` / `.tr-bang` / `.tr-type` / `.tr-dest` / `.tr-arr` / `.tr-left`
のいずれかに限定されている。
**要素型セレクタ・既存クラス・`*` は1つも無い**ので、既存レイアウトへの波及はない。

---

## 6. まだ検証できていないこと

| 項目 | 理由 | 確認方法 |
|---|---|---|
| JS 構文エラーの有無 | この環境に node/bun/deno が無い | ブラウザで開いて F12 コンソール |
| 実際の描画・レイアウト | 同上 | `CHECK_tracker_2.md` |
| 既存14機能の実行時挙動 | 静的には無変更を確認済み。実行確認は未 | `CHECK_tracker_2.md` |
| 遅延マークの実表示 | 実際に路線が遅延していないと出ない | 遅延発生時 |

`_selftest.html` の 21 アサーションは
`tw_build/tests/test_tracker_logic.py` で機械検証済み（アルゴリズムは確定）。
ブラウザでしか分からないのは上記の構文エラーと描画だけ。

---

## 7. 未決の設計判断（ユーザー確認が必要）

**到着駅に行かない列車をリストに出すか。**

現状は 305 件が到着列 `--:--` で表示される。

- G1 の「特急 新宿行き」は相模大野を通過するので、
  乗り継ぎ用途では使えないが、新宿へ直行する手段としては有効
- K3 の「各停 本厚木行き」は秦野まで行かないので、帰宅用途では使えない

出したままにするか、`--:--` の行を隠す／灰色にするかは仕様判断。
**現状は「出す」で実装済み**（`SPEC_tracker.md` の設計どおり）。
変更するなら指示をください。
